# Supabase project: goyzgiwtjdqmeystvums  (Project Settings -> API)
SUPABASE_URL=https://goyzgiwtjdqmeystvums.supabase.co
SUPABASE_SERVICE_ROLE_KEY=       # service_role secret — backend only, NEVER in frontend
# User JWTs are verified against the project's JWKS (ES256), fetched live from
# SUPABASE_URL — no shared secret needed.

FRONTEND_ORIGIN=http://localhost:5173
SCORING_K=5.0
